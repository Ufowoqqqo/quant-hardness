# Raw runs

Store immutable raw experimental outputs here, including per-query
measurements, resolved configuration, command, commit, seeds, implementation
revisions, and machine information. Never delete or overwrite a recorded run.

Large-result storage and retention policy have not yet been selected; do not
silently exclude raw runs from version control before that decision is made.

